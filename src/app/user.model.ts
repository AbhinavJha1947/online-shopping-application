// user.model.ts

export class User {
    id!: number;
    username!: string;
    password!: string;
    // Add any other user-related properties here (e.g., shopping cart, order history, etc.)
  }
  